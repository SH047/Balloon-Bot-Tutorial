Edit this wiring reference. Keep the upper six GPIO connections, L298N motor output connections, both wheel motor labels, title and the entire isolated 18650 panel unchanged. COMPLETELY REPLACE THE MAIN POWER-WIRING AREA with a clean net-label schematic to eliminate ALL wrong crossed/shorted connections. Do not run any long wires in the power section.
Specifically erase EVERY existing wire below the six GPIO wires connecting ESP32 power, L298N power, battery, buck and capacitor. Erase old battery and buck art and redraw as simple clean labeled rectangles.
Use short isolated labeled terminal stubs, with IDENTICAL NET LABELS meaning electrical connection. Three nets ONLY: +12V, +5V, GND. +12V and +5V and GND NEVER joined.
ESP32 5V terminal: short red stub left or right with label "+5V". ESP32 GND terminal: short black stub with label "GND".
L298N 12V / VS terminal: short red downward stub labeled "+12V". L298N GND terminal: short black downward stub labeled "GND". L298N 5V terminal: no connection, label "NC*".
Power panel below: battery rectangle with + terminal stub labeled "+12V" and − terminal stub labeled "GND". Explicitly BOTH stubs connect to their respective terminals.
Buck rectangle: "BUCK • SET TO 5.0V". Four short stubs: IN+ labeled "+12V"; IN− labeled "GND"; OUT+ labeled "+5V"; OUT− labeled "GND".
Capacitor inset: two parallel horizontal rails. Upper red rail labeled "+5V → ESP32 5V", lower black rail labeled "GND → ESP32 GND". One vertical polarized capacitor C1 bridges the rails, upper lead +, lower lead −. Label "1000 µF / ≥10V". Caption "Connect across 5V and GND near ESP32".
Power panel explanatory legend: "Matching net labels are connected together." "+12V: battery +, buck IN+, L298N VS" "+5V: buck OUT+, ESP32 5V, capacitor +" "GND: battery −, buck IN−/OUT−, ESP32 GND, L298N GND, capacitor −". Draw NO extra connecting wires between blocks; ONLY terminal stubs and these clear matching net labels. No short circuits, no mixing +12V and +5V. Preserve footer note about onboard regulator and USB. Image should be clean and all text legible.
